import React, { useEffect, useRef, useState } from 'react';
import { View, Text } from 'react-native';

interface Props {}

const Tracking: React.FC<Props> = () => {
  return (
    <View>
      <Text>Tracking landing</Text>
    </View>
  );
};

export default Tracking;